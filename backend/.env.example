# --- Server ---
PORT=4000
JWT_SECRET=change-me-in-prod

# --- Database ---
DATABASE_URL="postgresql://user:password@localhost:5432/rani?schema=public"

# --- Stellar (testnet) — values from stellarX-workshop-main/dev_setup ---
SOROBAN_RPC=https://soroban-testnet.stellar.org
HORIZON_URL=https://horizon-testnet.stellar.org
NETWORK=testnet

# Circle USDC issuer used by Soroswap / SDEX / most generic testnet flows.
# NOTE: Blend uses a DIFFERENT USDC issuer (GATALTGT...) — confirm before mixing protocols.
USDC_ISSUER=GBBD47IF6LWK7P7MDEVSCWR7DPUWV3NY3DTQEVFL4NAT4AQH3ZLLFLA5

FRIENDBOT_URL=https://friendbot.stellar.org
